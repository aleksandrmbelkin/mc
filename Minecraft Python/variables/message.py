from mcpi.minecraft import Minecraft
mc = Minecraft.create()
import time

s = да
answer = input("Создать кратер? Д/Н")
if answer == s 

pos = mc.player.getPos()
ms.setBlocks(pos.x + 1, pos.y + 1, pos.z + 1,
             
score = 0
pos = mc.player.getPos()
blockAbove = (pos.x, pos.y + 2, pos.z)
             pos.x - 1, pos.y - 1, pos.z - 1, 0)
mc.postToChat("Бабах!")
